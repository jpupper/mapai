# ============================================================
# CONFIGURACIÓN DE NGINX PARA DIPLOIA
# ============================================================
# IMPORTANTE: Agrega estas líneas DENTRO del bloque server {} 
# de tu archivo de configuración de Nginx.

    # APLICACIÓN: mapai (puerto 4560)
    location /mapai {
        # Proxy a Node.js (El servidor Node ya maneja el prefijo /mapai)
        proxy_pass http://localhost:4560;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header Cookie $http_cookie;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
